# scripts

Root-level scripts should be small utilities that do not belong strictly to `backend/scripts`.

Suggested layout:

- `scripts/oneoff/` one-time migration or cleanup helpers
- `scripts/dev/` local developer helpers
